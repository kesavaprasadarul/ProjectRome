<Page
    x:Class="ProjectRome.Views.StartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:locals="using:ProjectRome.Controls"
    xmlns:local ="using:ProjectRome.Views"
    xmlns:vmx ="using:Windows.System.RemoteSystems"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="pRing" TargetType="locals:ProgressBarRing">
            <Setter Property="Background" Value="Beige"/>
            <Setter Property="Foreground" Value="Blue"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="1"/>
            <Setter Property="Width" Value="48"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="locals:ProgressBarRing">
                        <Grid Background="{TemplateBinding Background}" CornerRadius="24">
                            <locals:ProgressBarRingSlice x:Name="Indicator" CacheMode="BitmapCache" Stroke="{TemplateBinding Foreground}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" InnerRadius="20" Radius="22" StartAngle="0" EndAngle="0">
                                <Path.RenderTransform>
                                    <RotateTransform x:Name="Rotation" CenterX="22" CenterY="22" Angle="0"/>
                                </Path.RenderTransform>
                            </locals:ProgressBarRingSlice>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">        
            <!--<Button x:Name="sendBtn" Content="Send" Click="sendBtn_Click"/>-->
            <TextBlock x:Name="fileNameTb" Text="{x:Bind originalFilename, Mode=OneWay}" FontWeight="SemiBold"/>
            <TextBlock x:Name="fileSizeTb" Text="{x:Bind transferProgress, Mode=OneWay}" FontWeight="Light"/>
            <ProgressBar Width="200" Value="{x:Bind transferProgress, Mode=OneWay}" Maximum="1"/>
            <TextBox x:Name="addressBar"/>
            <Button Content="Warp Link" x:Name="sendLinkBtn" Margin="0,4" Click="sendLinkBtn_Click" HorizontalAlignment="Center"/>
            <Button Content="Send File" x:Name="sendBtn" Margin="0,4" Click="sendBtn_Click" HorizontalAlignment="Center"/>
            <ListView x:Name="remoteSystemList" Margin="12,12" Foreground="Wheat" Background="Gray" SelectionChanged="ListBox_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vmx:RemoteSystem">
                        <TextBlock Text="{x:Bind DisplayName}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>


        </StackPanel>
    </Grid>
</Page>
